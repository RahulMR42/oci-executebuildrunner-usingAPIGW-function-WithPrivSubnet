version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash

steps:
  - type: Command
    name: "test step"
    timeoutInSeconds: 40
    command: |
      cd ${OCI_WORKSPACE_DIR}
      ls -ltr 
      
  
  